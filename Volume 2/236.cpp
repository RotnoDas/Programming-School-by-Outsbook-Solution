#include <bits/stdc++.h>
using namespace std;
int main ()
{
    int S, L;
    cin >> S >> L;
    double x = sqrt(pow(S, 2 ) + pow(L, 2));
    cout << fixed << setprecision(2);
    cout << x;
    return 0;
}
